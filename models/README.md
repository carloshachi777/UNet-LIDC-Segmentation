# Trained Models

Trained Keras model checkpoints are not stored directly in this repository because of their file size.

The repository provides the complete preprocessing, training, evaluation, configuration, random-seed, split, and result metadata required to reproduce the reported models.

Expected local structure:

models/

├── seed1/
│   └── best_plos_2d_unet.keras
├── seed2/
│   └── best_plos_2d_unet.keras
└── seed42/
    └── best_plos_2d_unet.keras
